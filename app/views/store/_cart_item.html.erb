<% if cart_item == @current_item %>
  <tr id ="current_item">
<% else %>
  <tr>
<% end %>
  <td>"Cart Item" </td>
  <td><%= cart_item.quantity%>&times; </td>
  <td><%=h cart_item.title %> </td>
  <td calss="item-price"><%= number_to_currency(cart_item.price) %> </td>
</tr>
